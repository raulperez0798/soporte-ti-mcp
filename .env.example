OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-5.4-nano

# Opcional: si se configura, la app se conecta al MCP remoto por HTTP.
# Si se deja vacio/sin definir, se usa el MCP local via stdio (subproceso).
MCP_SERVER_URL=
